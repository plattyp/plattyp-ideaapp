<h1>Create Idea</h1>

<div class="row">
	<div class="col-md-6">
	<%= simple_form_for (@idea) do |i| %>
		<%= render "createidea", :i => i %>
		<%= i.button :submit, :value => "Continue", :class => 'btn-primary'%>
	<% end %>
	</div>
</div>
